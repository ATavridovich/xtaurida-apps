{
  "name": "xtform",
  "displayName": "XTForm Viewer",
  "description": "Visual viewer for .xtform files with form designer and property editor",
  "version": "0.1.0",
  "publisher": "xtaurida",
  "license": "MIT",
  "engines": {
    "vscode": "^1.70.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCustomEditor:xtform.editor"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "customEditors": [
      {
        "viewType": "xtform.editor",
        "displayName": "XTForm Viewer",
        "selector": [
          {
            "filenamePattern": "*.xtform"
          }
        ],
        "priority": "exclusive"
      }
    ]
  },
  "scripts": {
    "compile": "node esbuild.extension.mts && node esbuild.webview.mts",
    "watch": "node esbuild.extension.mts --watch",
    "pretest": "npm run compile",
    "test": "node ./out/test/runTest.js",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "glob": "^13.0.6",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.0",
    "@types/node": "24.x",
    "@types/vscode": "^1.70.0",
    "esbuild": "^0.18.0",
    "mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
